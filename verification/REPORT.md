# Verification Record

Date: 2026-09-10. This report describes checks performed on the delivered code. It does not establish reproduction of experiments on real datasets.

## Completed Checks

| Check | Result |
| --- | --- |
| Core numerical and module tests | 16 passed; see `unit_tests.txt` |
| CUDA tests | Three skipped because no GPU was available; not counted as passes |
| End-to-end training | Synthetic phantom, CPU, 16 x 16 images, three optimizer updates; finite losses and gradients |
| Checkpoint saving/loading | `last.pt` saved and successfully loaded in `weights_only` mode |
| Optimizer-state resume | Resumed from step three and completed the fourth update |
| Single-view DDIM inference | Three sampling steps; exported 150 active Gaussians; repeatable with the same seed |
| Arbitrary-view rendering | Successfully rendered six calibrated angles from the same exported Gaussian field |
| Volume generation and evaluation | Exported a 16^3 volume and evaluated test-scene projections and volume |
| Scanner data conversion | Camera scaling, projection preservation, attenuation-unit conversion, and input/target index checks passed |
| Main ablation branches | Finite forward outputs for attention, static_state, PSR, and static_state+IGA |
| Configuration loading | All YAML inheritance chains and key dimension checks passed |
| Full-configuration instantiation | 46,551,209 parameters; 1,048,576 candidates before routing |

Core tests cover log-SNR reliability monotonicity; associative versus sequential recurrence outputs and gradients; state copying for inactive subspaces; dense-training versus sparse-inference forward equivalence; minimum capacity and nested gates; positive-definite covariance; existence gradients for candidates with zero forward attenuation; analytic ray integration; finite-difference checks of integration gradients; linear attenuation accumulation; DDIM updates and terminal-step handling; empty-query bypass; gradients of key PSR/IGA parameters; gradient checkpointing; repeatable sampling and memory reset; and rejection of patient split leakage.

`smoke_train.jsonl`, `smoke_resume.jsonl`, `smoke_metrics.json`, `ablation_checks.json`, `data_conversion.json`, and `environment.json` contain the actual outputs. `smoke_metrics.json` records synthetic-data results after only three training updates and must not be compared with the manuscript tables.

## Not Yet Executed

- CUDA extension compilation and numerical checks of zero-density gradients and voxelization.
- Full-configuration training, throughput, memory use, and 20-step inference performance measurements on an RTX 4090 with 24 GB memory.
- Training on the official LIDC-IDRI/Xt3D partitions, validation-based model selection, and test evaluation.
- Nodule and small-structure metrics using real annotations and a fixed segmentation evaluator.
- Reproduction of the manuscript's 112.1M parameter count, FLOPs, reconstruction quality, and latency.

The tiny synthetic checkpoint used for these checks is not bundled, to avoid confusion with a manuscript-trained model. It can be regenerated by following the minimal workflow in the README.
